# Scripts

`scripts/*` contains ad-hoc CLI utilities and tests used during development.

Examples:
- backtest / engine tests
- smoke scripts

Rule: scripts may be more permissive, but should not introduce production-only dependencies.
